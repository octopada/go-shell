GoSh

Instructions
 1. gcc shell.c
 2. ./a.out

Notes:
 Builtins are - cd, pwd, echo
 echo is basic, only prints following string (whitespaces replaced by spaces)
 cd ~ changes to home directory
 exit quits
